name: Walidacja HACS

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - name: Walidacja repozytorium jako integracji
        uses: hacs/action@main
        with:
          category: integration
